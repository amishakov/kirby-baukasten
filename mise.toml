[tools]
node = "24"
php = "8.5.9"
pnpm = "11.20.0"

[tasks.setup]
description = "Install project dependencies and create the local environment file"
run = [
	"composer install",
	"composer global require getkirby/cli",
	"pnpm install --frozen-lockfile",
	"test -f .env || cp .env.example .env"
]
